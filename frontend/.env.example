# PROTEUS Frontend — Environment Variables
# Copy this file to .env and fill in the values

# Backend API URL
# Development: leave empty (Vite proxy handles /api → localhost:8000)
# Production: set to your Railway backend URL
VITE_API_URL=https://proteus-api.up.railway.app
