name: Model Health Check

on:
  schedule:
    - cron: "0 */3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  health-check:
    name: Check NIM & Groq Models
    runs-on: ubuntu-latest
    env:
      NVIDIA_NIM_API_KEY: ${{ secrets.NVIDIA_NIM_API_KEY }}
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Run model health check
        id: health
        run: node scripts/model-health.mjs
        continue-on-error: true

      - name: Commit changes if models were swapped
        if: steps.health.outcome == 'success'
        run: |
          git config user.name "proteus-bot"
          git config user.email "bot@proteus.app"
          git add models.json README.md
          git diff --staged --quiet || git commit -m "chore: auto-update model health check [skip ci]"
          git push
